#In this task, define a variable called login_attempts that represents the current number of 
#login attempts made by a user. Store the value 2 in this variable. Then, store its data type 
#in a variable called login_attempts_type. Display login_attempts_type to observe the output.

login_attempts = 2
login_attempts_type = type(login_attempts)
print(login_attempts_type)
# Output: <class 'int'>