#In this task, define a variable called max_logins that represents the maximum number of login 
#attempts allowed per user. Store the value 3 in this variable. Then, store its data type in another 
#variable called max_logins_type. Display max_logins_type to examine the output.

max_logins = 3
max_logins_type = type(max_logins)
print(max_logins_type)
# Output: <class 'int'>