#Now that the variable device_id is defined, you can return its data type.In this task, use a 
#Python function to find the data type of the variable device_id. Store the data type in another 
#variable called device_id_type. Then, display device_id_type to examine the output.

device_id_type = type(device_id)
print(device_id_type)
# Output: <class 'str'>
