#In your work as an analyst, imagine there is a device only users specified on an allow list can 
#access, and its device ID is "72e08x0".In the following code cell, assign this value to a variable 
#named device_id. Then, display the contents of the variable and observe the output.

device_id = "72e08x0"
print(device_id)